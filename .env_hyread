# 桃園市立圖書館 HyRead 電子書登入設定

# 圖書館帳號（您的借閱證號）
HYREAD_ACCOUNT=A123339087

# 圖書館密碼
HYREAD_PASSWORD=dddd2222

# Google Gemini API 金鑰
OPENAI_API_KEY=AIzaSyC_RHCWws7k9AIs3lzmXdMX1px4MI89sYo

# Gemini 模型（使用原生 Gemini SDK，不需要 BASE_URL）
OPENAI_MODEL=gemini-2.5-flash

# 驗證碼辨識模式
# auto - 使用 Gemini API 自動辨識（需要 API Key）
# manual - 手動輸入驗證碼（不使用 API，節省費用）
CAPTCHA_MODE=auto

# 目標書籍 ID（從書籍 URL 中獲取，例如 bookDetail.jsp?id=279235）
HYREAD_BOOK_ID=198409

# 是否啟用爬蟲功能（抓取書籍內容）
# true - 自動爬取書籍內容並儲存為 Markdown
# false - 只借閱，不爬取內容
ENABLE_SCRAPING=true

# 最大爬取頁數（防止無限循環）
MAX_PAGES=1000

# 是否下載圖片到本地
# true - 下載圖片到 downloads/images/ 目錄
# false - 只保存圖片 URL
DOWNLOAD_IMAGES=true

# 純圖片書籍模式（全書都是 Canvas 圖片）
# true - 只抓取 Canvas 圖片（適合圖片書、漫畫）
# false - 正常抓取 HTML + Canvas（適合一般書籍）
IMAGE_ONLY_MODE=false

# 智能翻頁策略
# true - 根據章節剩餘頁數智能決定翻頁數量（加速抓取）
# false - 使用固定翻頁數量（穩定但較慢）
SMART_PAGE_TURN=true

# 固定翻頁數量（當 SMART_PAGE_TURN=false 時使用）
# 建議值：1-5（數字越大速度越快，但可能漏掉內容）
PAGES_PER_TURN=1

# 翻頁按鍵設定
# 可選值：ArrowRight (右鍵), ArrowLeft (左鍵), ArrowUp (上鍵), ArrowDown (下鍵)
# 預設：ArrowRight（大多數電子書使用右鍵翻到下一頁）
PAGE_TURN_KEY=ArrowLeft

